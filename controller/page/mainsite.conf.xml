
<blog>

	<!-- Configure where the engine should look to
	find entries for this blog/article collection -->
	<post_directory>/var/www/site-data/pages</post_directory>

	<page_directory>/var/www/site-data/pages</page_directory>

	<!-- Configure the id of the blog (this is where the
	 blog is on the web, i.e example.com/yourblog).
	 If you intend to use the builtin feed or sitemap
	 generator, you MUST make this the same name as the 
	 associated controller or generated urls will be wrong -->
	<id>page</id>

	<!-- Configure the title of the blog -->
	<title>Nate Levesque</title>

	<short_title>Nate</short_title>

	<!-- Configure the blog's (brief) description/catchline -->
	<catchline>Colloquially, TheNaterhood.</catchline>

	<!-- Insert any comment code (such as disqus) here -->
	<comment_code></comment_code>

</blog>
